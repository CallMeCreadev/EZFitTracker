<resources>
    <!-- Custom Toolbar title style -->
    <style name="CustomToolbarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">@Roboto/</item>
    </style>
    <style name="MyCustomActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">@drawable/gradient_topbar_silver</item>
        <!-- Additional style attributes -->
    </style>
</resources>
